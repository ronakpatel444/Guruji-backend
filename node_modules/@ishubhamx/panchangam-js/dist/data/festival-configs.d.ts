export declare const SOLAR_FESTIVALS: Record<number, Array<{
    name: string;
    type: 'single' | 'span';
    /** For single-type entries: how many days before (negative) or after (positive) Sankranti
     * this festival falls. Defaults to 0 (= on Sankranti day). */
    dayOffset?: number;
    spanDays?: number;
    dayNames?: string[];
    regional?: string[];
    description?: string;
}>>;
/**
 * Multi-Day Festival Spans Configuration
 *
 * These festivals span multiple consecutive Tithis.
 */
export declare const MULTI_DAY_FESTIVALS: Record<string, {
    name: string;
    masaIndex: number;
    startTithi: number;
    endTithi: number;
    spanDays: number;
    dailyNames: string[];
    description: string;
}>;
//# sourceMappingURL=festival-configs.d.ts.map