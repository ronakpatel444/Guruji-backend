import type { FestivalCategory } from '../types/festivals';
/**
 * Declarative rule describing when a festival occurs.
 *
 * Simple festivals (tithi-only) are fully described by masaIndex + tithi.
 * Complex rules (vara, nakshatra, kala conditions) require additional fields.
 */
export interface FestivalRule {
    /** Display name of the festival */
    name: string;
    /** Category for filtering/UI */
    category: FestivalCategory;
    /** Amanta masa index 0–11 (Chaitra=0 … Phalguna=11) */
    masaIndex: number;
    /** 1-indexed tithi (1–30) */
    tithi: number;
    /** Paksha filter (optional — omit to match both) */
    paksha?: 'Shukla' | 'Krishna';
    /** Required day of week 0=Sun…6=Sat (optional) */
    vara?: number;
    /** Required Nakshatra index 0–26 (optional) */
    nakshatra?: number;
    /** Kala when the tithi must prevail: 'sunrise' | 'midday' | 'sunset' */
    kala?: 'sunrise' | 'midday' | 'sunset';
    /** Whether this festival is observed in Adhika Masa */
    adhikaAllowed?: boolean;
    /** Vriddhi rule: 'first' | 'second' (which of two sunrises to use) */
    vriddhiRule?: 'first' | 'second';
    /** Traditions/regions where this festival is observed */
    regional?: string[];
    /** Free-text description */
    description?: string;
    /** Observances / rituals */
    observances?: string[];
    /** Whether a fast is observed */
    isFastingDay?: boolean;
}
//# sourceMappingURL=types.d.ts.map