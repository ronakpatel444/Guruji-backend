"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSamvat = getSamvat;
const samvatsara_1 = require("../data/samvatsara");
function getSamvat(date, masaIndex) {
    let yearAD = date.getUTCFullYear();
    let shaka = yearAD - 78;
    // If we are in Pausha/Magha/Phalguna (masaIndex 9-11) early in the calendar year,
    // we are still in the previous Shaka year.
    if (masaIndex > 8 && date.getUTCMonth() < 3) {
        shaka -= 1;
    }
    const vikram = shaka + 135;
    // 60-year Samvatsara cycle.
    // Formula: (shaka + 11) % 60 verified against Drik Panchang reference data.
    const samvatIndex = (shaka + 11) % 60;
    const samvatsara = samvatsara_1.samvatsaraNames[samvatIndex];
    return { vikram, shaka, samvatsara };
}
//# sourceMappingURL=samvat.js.map