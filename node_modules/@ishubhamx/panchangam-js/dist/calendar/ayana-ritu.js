"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAyana = getAyana;
exports.getRitu = getRitu;
const masa_1 = require("../data/masa");
function getAyana(sunLon) {
    // Sun tropical longitude.
    // 90-270: Dakshinayana (Cancer to Capricorn ingress)
    // 270-90: Uttarayana
    if (sunLon >= 90 && sunLon < 270) {
        return masa_1.ayanaNames[1]; // Dakshinayana
    }
    else {
        return masa_1.ayanaNames[0]; // Uttarayana
    }
}
function getRitu(sunLon) {
    // 6 Ritus, 60 degrees each starting from Pisces (330°)
    if (sunLon >= 330 || sunLon < 30)
        return masa_1.rituNames[0]; // Vasant
    if (sunLon >= 30 && sunLon < 90)
        return masa_1.rituNames[1]; // Grishma
    if (sunLon >= 90 && sunLon < 150)
        return masa_1.rituNames[2]; // Varsha
    if (sunLon >= 150 && sunLon < 210)
        return masa_1.rituNames[3]; // Sharad
    if (sunLon >= 210 && sunLon < 270)
        return masa_1.rituNames[4]; // Hemant
    return masa_1.rituNames[5]; // Shishir
}
//# sourceMappingURL=ayana-ritu.js.map