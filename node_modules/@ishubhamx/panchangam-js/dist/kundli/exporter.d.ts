import { KundliConfig } from "./types";
export interface ExportProfile {
    name: string;
    dateStr: string;
    latitude: number;
    longitude: number;
    elevation?: number;
    timezoneOffsetMinutes?: number;
}
/**
 * Computes the complete Kundli for a given profile and exports it to a JSON file.
 *
 * @param profile Birth details and target information
 * @param outputPath File path to write the JSON to
 * @param config Optional Kundli configuration (e.g., house system)
 * @returns The generated JSON string
 */
export declare function exportKundliToJson(profile: ExportProfile, outputPath: string, config?: KundliConfig): string;
//# sourceMappingURL=exporter.d.ts.map