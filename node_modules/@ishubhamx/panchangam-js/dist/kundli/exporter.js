"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.exportKundliToJson = exportKundliToJson;
const astronomy_engine_1 = require("astronomy-engine");
const index_1 = require("./index");
const fs = __importStar(require("fs"));
/**
 * Computes the complete Kundli for a given profile and exports it to a JSON file.
 *
 * @param profile Birth details and target information
 * @param outputPath File path to write the JSON to
 * @param config Optional Kundli configuration (e.g., house system)
 * @returns The generated JSON string
 */
function exportKundliToJson(profile, outputPath, config = {}) {
    const date = new Date(profile.dateStr);
    const elevation = profile.elevation ?? 10.0;
    const observer = new astronomy_engine_1.Observer(profile.latitude, profile.longitude, elevation);
    const kundli = (0, index_1.getKundli)(date, observer, config);
    const payload = {
        meta: {
            name: profile.name,
            inputDate: profile.dateStr,
            latitude: profile.latitude,
            longitude: profile.longitude,
            elevation,
            timezoneOffset: profile.timezoneOffsetMinutes ?? 0,
            generatedAt: new Date().toISOString()
        },
        kundli
    };
    const jsonString = JSON.stringify(payload, null, 2);
    fs.writeFileSync(outputPath, jsonString, "utf8");
    return jsonString;
}
//# sourceMappingURL=exporter.js.map