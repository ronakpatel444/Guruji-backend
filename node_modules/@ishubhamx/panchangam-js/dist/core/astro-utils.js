"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SPEED_CALC_HALF_WINDOW_MS = exports.SANKRANTI_ADVANCE_MS = exports.SANKRANTI_LOOKBACK_MS = exports.SANKRANTI_SEARCH_WINDOW_MS = exports.MS_PER_YEAR = exports.DAYS_PER_CENTURY = exports.JD_J2000 = exports.JD_UNIX_EPOCH = exports.NAKSHATRA_LOOKBACK_MS = exports.BINARY_SEARCH_LOOKAHEAD_MS = exports.BINARY_SEARCH_MAX_ITERATIONS = exports.MS_PER_MINUTE = exports.MS_PER_HOUR = exports.MS_PER_DAY = void 0;
exports.guessTimezoneOffset = guessTimezoneOffset;
exports.getStartOfLocalDay = getStartOfLocalDay;
exports.search = search;
exports.getPlanetaryDignity = getPlanetaryDignity;
exports.getJulianCenturies = getJulianCenturies;
const constants_1 = require("./constants");
// ===== Named Constants =====
/** Milliseconds in one day */
exports.MS_PER_DAY = 24 * 60 * 60 * 1000;
/** Milliseconds in one hour */
exports.MS_PER_HOUR = 60 * 60 * 1000;
/** Milliseconds in one minute */
exports.MS_PER_MINUTE = 60 * 1000;
/** Maximum iterations for binary search convergence */
exports.BINARY_SEARCH_MAX_ITERATIONS = 20;
/** Lookahead window for binary search (2 days in ms) */
exports.BINARY_SEARCH_LOOKAHEAD_MS = 2 * exports.MS_PER_DAY;
/** Lookback window for finding nakshatra start (32 hours in ms) */
exports.NAKSHATRA_LOOKBACK_MS = 32 * exports.MS_PER_HOUR;
/** Julian Day of Unix Epoch (1970-01-01T00:00:00 UTC) */
exports.JD_UNIX_EPOCH = 2440587.5;
/** Julian Day of J2000.0 epoch */
exports.JD_J2000 = 2451545.0;
/** Days per Julian century */
exports.DAYS_PER_CENTURY = 36525.0;
/** Milliseconds per Gregorian year (365.25 days) */
exports.MS_PER_YEAR = 365.25 * exports.MS_PER_DAY;
/** Sankranti search window (40 days in ms) */
exports.SANKRANTI_SEARCH_WINDOW_MS = 40 * exports.MS_PER_DAY;
/** Sankranti search lookback (35 days in ms) */
exports.SANKRANTI_LOOKBACK_MS = 35 * exports.MS_PER_DAY;
/** Advance to next Sankranti (~25 days in ms) */
exports.SANKRANTI_ADVANCE_MS = 25 * exports.MS_PER_DAY;
/** Finite difference half-window for speed calculation (30 minutes in ms) */
exports.SPEED_CALC_HALF_WINDOW_MS = 30 * exports.MS_PER_MINUTE;
/**
 * Approximates timezone offset in **minutes** from observer longitude,
 * rounded to the nearest 30-minute slot (e.g. +330 for IST from ~82°E).
 * Use this as a fallback when the caller does not supply an explicit offset.
 */
function guessTimezoneOffset(observer) {
    const rawOffsetHours = observer.longitude / 15.0;
    const roundedHalfHours = Math.round(rawOffsetHours * 2) / 2; // nearest 0.5h
    return roundedHalfHours * 60; // minutes
}
function getStartOfLocalDay(date, observer, options) {
    // Use the explicitly provided offset; fall back to longitude-based approximation.
    const tzOffsetMs = (options && options.timezoneOffset !== undefined)
        ? options.timezoneOffset * exports.MS_PER_MINUTE
        : guessTimezoneOffset(observer) * exports.MS_PER_MINUTE;
    // Create a date shifted to "Observer Local Time"
    const localDate = new Date(date.getTime() + tzOffsetMs);
    localDate.setUTCHours(0, 0, 0, 0); // Set to Local Midnight
    // Shift back to UTC to get the actual UTC timestamp of Local Midnight
    const startOfDay = new Date(localDate.getTime() - tzOffsetMs);
    // End of day is 24 hours later
    const endOfDay = new Date(startOfDay.getTime() + exports.MS_PER_DAY - 1);
    return { start: startOfDay, end: endOfDay };
}
/**
 * A generic search function to find the time when a function f(t) crosses zero.
 * It uses a binary search approach.
 */
function search(f, startDate) {
    let a = startDate;
    let b = new Date(startDate.getTime() + exports.BINARY_SEARCH_LOOKAHEAD_MS);
    let fa = f(a);
    let fb = f(b);
    if (fa * fb >= 0) {
        return null;
    }
    for (let i = 0; i < exports.BINARY_SEARCH_MAX_ITERATIONS; i++) {
        const m = new Date((a.getTime() + b.getTime()) / 2);
        const fm = f(m);
        if (fm * fa < 0) {
            b = m;
            fb = fm;
        }
        else {
            a = m;
            fa = fm;
        }
    }
    return a;
}
function getPlanetaryDignity(planet, rashi) {
    if (constants_1.planetExaltation[planet] === rashi)
        return 'exalted';
    if (constants_1.planetDebilitation[planet] === rashi)
        return 'debilitated';
    if (constants_1.planetOwnSigns[planet]?.includes(rashi))
        return 'own';
    return 'neutral';
}
// Julian Centuries from J2000.0
function getJulianCenturies(date) {
    const jd = (date.getTime() / (exports.MS_PER_DAY)) + exports.JD_UNIX_EPOCH;
    return (jd - exports.JD_J2000) / exports.DAYS_PER_CENTURY;
}
//# sourceMappingURL=astro-utils.js.map