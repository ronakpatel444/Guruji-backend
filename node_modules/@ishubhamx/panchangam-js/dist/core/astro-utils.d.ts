/**
 * Shared astronomical constants and low-level utilities for the core calculation modules.
 * These are internal helpers; not all of these are part of the public package API.
 */
import { Observer } from 'astronomy-engine';
/** Milliseconds in one day */
export declare const MS_PER_DAY: number;
/** Milliseconds in one hour */
export declare const MS_PER_HOUR: number;
/** Milliseconds in one minute */
export declare const MS_PER_MINUTE: number;
/** Maximum iterations for binary search convergence */
export declare const BINARY_SEARCH_MAX_ITERATIONS = 20;
/** Lookahead window for binary search (2 days in ms) */
export declare const BINARY_SEARCH_LOOKAHEAD_MS: number;
/** Lookback window for finding nakshatra start (32 hours in ms) */
export declare const NAKSHATRA_LOOKBACK_MS: number;
/** Julian Day of Unix Epoch (1970-01-01T00:00:00 UTC) */
export declare const JD_UNIX_EPOCH = 2440587.5;
/** Julian Day of J2000.0 epoch */
export declare const JD_J2000 = 2451545;
/** Days per Julian century */
export declare const DAYS_PER_CENTURY = 36525;
/** Milliseconds per Gregorian year (365.25 days) */
export declare const MS_PER_YEAR: number;
/** Sankranti search window (40 days in ms) */
export declare const SANKRANTI_SEARCH_WINDOW_MS: number;
/** Sankranti search lookback (35 days in ms) */
export declare const SANKRANTI_LOOKBACK_MS: number;
/** Advance to next Sankranti (~25 days in ms) */
export declare const SANKRANTI_ADVANCE_MS: number;
/** Finite difference half-window for speed calculation (30 minutes in ms) */
export declare const SPEED_CALC_HALF_WINDOW_MS: number;
export interface LocalDayOptions {
    timezoneOffset?: number;
}
/**
 * Approximates timezone offset in **minutes** from observer longitude,
 * rounded to the nearest 30-minute slot (e.g. +330 for IST from ~82°E).
 * Use this as a fallback when the caller does not supply an explicit offset.
 */
export declare function guessTimezoneOffset(observer: Observer): number;
export declare function getStartOfLocalDay(date: Date, observer: Observer, options?: LocalDayOptions): {
    start: Date;
    end: Date;
};
/**
 * A generic search function to find the time when a function f(t) crosses zero.
 * It uses a binary search approach.
 */
export declare function search(f: (date: Date) => number, startDate: Date): Date | null;
export declare function getPlanetaryDignity(planet: string, rashi: number): 'exalted' | 'debilitated' | 'own' | 'neutral';
export declare function getJulianCenturies(date: Date): number;
//# sourceMappingURL=astro-utils.d.ts.map